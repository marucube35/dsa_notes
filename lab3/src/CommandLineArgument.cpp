#include "Sorting.h"
#include "Data.h"
#include "cmd.h"

//Main process of command line argument
int main(int argc, const char* argv[])
{
    commandLineProcess(argc, argv);
    system("pause");
    return 0;
}
